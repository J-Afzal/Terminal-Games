[project]
name = "terminal-games"
version = "1.0.0"
description = "Simple games that run in the terminal"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.12.7",
    "yamllint>=1.37.1",
]

[tool.ruff]
line-length = 128
